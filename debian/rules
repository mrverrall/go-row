#!/usr/bin/make -f

URL = github.com/mrverrall/go-row
CFLAGS := $(CFLAGS)

%:
	dh $@

override_dh_auto_clean:
override_dh_auto_test:
override_dh_auto_build:
override_dh_auto_install:
	go get $(URL)
	go build -o debian/bin/go-row
